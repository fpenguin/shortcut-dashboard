# macOS
.DS_Store

# Local temp/cache output
*.tmp
*.log
*.bak
*~
.cache/
.pytest_cache/
__pycache__/
*.pyc
tmp/
temp/
source-cache/
shortcut-dashboard-source-cache/
current_manifest_scan.json
manifest.tsv

# Personal shortcut data and exports
shortcuts.json
*.bttpreset

# Agent/session handoff notes (private; never commit)
# Both cases listed so it stays ignored on case-sensitive filesystems (Linux) too.
handoff.md
HANDOFF.md

# Keep publishable examples
!shortcuts_example.json
!example.json
